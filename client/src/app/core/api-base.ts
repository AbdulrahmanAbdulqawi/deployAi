// Overwritten by scripts/write-api-env.mjs at deploy time when NG_APP_API_URL / API_URL is set.
// Empty means requests stay relative and rely on the vercel.json proxy rewrites in production.
export const API_BASE_URL = '';
