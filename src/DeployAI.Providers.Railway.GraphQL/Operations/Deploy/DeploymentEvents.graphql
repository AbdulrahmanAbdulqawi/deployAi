query DeploymentEvents($id: String!, $first: Int) {
  deploymentEvents(id: $id, first: $first) {
    edges {
      node {
        step
        completedAt
        payload {
          error
          reason
          detail
        }
      }
    }
  }
}
